func count_lines(text: &String): usize {
    let bytes = text.bytes()
    var lines: usize = 0
    var offset: usize = 0
    while offset < bytes.len() {
        if bytes[offset] == 10 {
            lines = lines + 1
        }
        offset = offset + 1
    }
    return lines
}
