use std/io
use std/net.SocketAddress

func main(): i32! {
    let address = SocketAddress.parse("[::1]:443") otherwise {
        return error.new("network-address.invalid", "loopback address did not parse")
    }
    let text = address.to_string()
    io.println(&text)?
    return 0
}
