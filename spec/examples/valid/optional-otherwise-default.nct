// check-valid: `std/process.env` is check-only until runtime environment
// lowering is promoted.
use std/process.env

func main(): i32! {
    let user = env("USER")? otherwise { "unknown" }

    use_name(user)
    return 0
}

func use_name(name: &str): void {
    return
}
