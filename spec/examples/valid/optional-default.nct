use std/prelude

from std/process import env

program(): i32 {
    let user = (env("USER") catch error {
        return 1
    }) ?? "unknown"

    use_name(user)
    return 0
}

func use_name(name: str): void {
    return
}
