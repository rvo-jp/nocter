use std/prelude

program(): i32 {
    let value = lookup()

    // invalid: postfix `?` on T? requires an optional return layer
    let number = value?

    return number
}

func lookup(): i32? {
    return none
}
