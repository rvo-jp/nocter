# v0.26.0: Monotonic Time Foundation

**Status: Phase 0 complete; implementation in progress (2026-09-02).** v0.26.0 adds one bounded
application foundation: normalized durations, monotonic elapsed-time measurement, and blocking
sleep. It does not add calendar time, async execution, or another guarantee modifier.

## Completion Boundary

- `std/time.Duration` represents every normalized non-negative span through `u64` whole seconds
  and a subsecond nanosecond component;
- constructors avoid intermediate overflow, while addition and subtraction report unrepresentable
  results through absence rather than trapping;
- `Instant.now` and `Instant.elapsed` use a monotonic target counter without exposing its ticks or
  frequency;
- one complete counter cycle exceeds one hundred years, and elapsed subtraction handles one wrap;
- `time.sleep` rounds positive sub-resolution waits upward, retries interruption from a freshly
  measured remainder, chunks values beyond one target wait, and reports other OS failures;
- all public operations publish `noalloc`, while sleep deliberately makes no non-blocking or
  realtime promise;
- formatter, hover, completion, navigation, installed-standard checking, and native execution see
  the same public contracts;
- complete workspace, native, documentation, and design-review gates pass.

## Architecture Boundary

`nocter-runtime-contract` owns three closed target primitive roles: monotonic counter read, fixed
counter frequency, and wrapping counter delta. The ARM64 backend implements those roles directly
from architectural counter state. Target validation fixes their semantic signatures and effect
evidence; the standard library cannot select them by name.

`std/internal/os/darwin` owns the raw timeout structure and exactly one descriptor-free target wait.
`std/time` owns counter conversion, normalized value invariants, rounding, chunking, retry, and
public errors. The compiler does not know `Duration` layout or sleep policy, and the OS adapter does
not know the public time model.

Wall-clock `gettimeofday` is not a fallback for monotonic time because clock adjustment can make it
move backward. Dynamic calls to `libSystem`, platform SDK dependencies, and target spelling checks
are prohibited.

## Phase 0: Contract and Feasibility

**Status: complete.** The normative contract fixes normalization, checked arithmetic, counter-wrap
behavior, resolution rounding, interruption retry, failure codes, `noalloc` scope, and responsibility
boundaries. ARM64 Darwin can supply the counter without an external runtime; its existing syscall
boundary can perform descriptor-free timed waits.

## Phase 1: Target Primitive Contract

**Status: in progress.** Add exact primitive roles, declaration locators, target signature
validation, ARM64 selection, closed instruction encoding, and unit tests. No public time type enters
the compiler.

## Phase 2: Standard Time Values

**Status: pending.** Add `std/time` contract and implementation sources for `Duration` and `Instant`,
including boundary tests and private target primitive declarations.

## Phase 3: Blocking Sleep

**Status: pending.** Add the Darwin one-wait adapter and target-independent sleep loop with rounding,
chunking, interruption policy, and native elapsed-time tests.

## Phase 4: Tooling and Practical Integration

**Status: pending.** Add public examples, specification cross-links, LSP coverage, installed-standard
qualification, and any stable API documentation needed to use the module without reading its body.

## Phase 5: Qualification and Review

**Status: pending.** Run the complete workspace and native gates. Review for duplicate time
conversion, wall-clock fallback, machine-to-semantic reversal, target ABI leakage, caller-discipline
preconditions, effect overstatement, and obsolete wrappers before declaring the milestone complete.
