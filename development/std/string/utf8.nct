//! UTF-8 validation shared by owned-string construction.

see ./index.nct

use /internal/utf8.is_valid_utf8 as validate_utf8

func is_valid_utf8(candidate: &[u8]): bool {
    return validate_utf8(candidate)
}
