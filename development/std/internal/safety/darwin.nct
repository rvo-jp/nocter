//! Darwin implementation of standard-library safety termination boundaries.

see ./index.nct

use /internal/os/darwin

#target: "arm64-darwin"
noalloc func bounds_abort(): never {
    return darwin.trap()
}

#target: "arm64-darwin"
noalloc func invariant_abort(): never {
    return darwin.trap()
}
