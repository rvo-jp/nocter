//! Borrowed process standard-input construction.

see ./index.nct
see ./file.nct

use /internal/os/darwin

noalloc func stdin(): BlockingFile {
    return BlockingFile { fd: darwin.STDIN_FD, state: BlockingFileState.borrowed_open }
}
