//! Shared validation of one absolute file position.

see ./index.nct

noalloc func absolute_file_position(position: i64): i64! {
    if position < 0 {
        return error.new("std.io.offset_out_of_range", "file offset is out of range")
    }
    return position
}
