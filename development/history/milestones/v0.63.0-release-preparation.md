# v0.63.0 Release Preparation

**Status: qualified candidate retained; publication pending.** This record owns the v0.63.0
release-content identity, qualification contract, and retained local artifact evidence. v0.62.0
remains the latest published release until publication is completed and externally audited.

## Release Boundary

v0.63.0 closes operating-system cryptographic entropy, transactional random-byte filling,
full-width and unbiased bounded unsigned generation, and allocation-free in-place shuffling. The
[milestone](v0.63.0.md) owns the completed phase scope. The
[Phase 3 review](../reviews/v0.63.0-phase-3.md) records the final responsibility model and resolved
findings. Public candidate notes are
[`../../../releases/v0.63.0.md`](../../../releases/v0.63.0.md).

## Candidate Identity

The release inputs agree on `0.63.0`:

- `development/packaging/VERSION`, the sole authored release-version input;
- the `#package` version in `std/index.nct`;
- the archive name and installed manifest derived from `VERSION` and `RELEASE.json`; and
- compiler and language-server reporting derived from the validated installed home.

`RELEASE.json` remains the machine authority for version-independent license, host, target, and
archive-layout metadata. Qualification verifies that its license agrees with the shipped legal
files and all Cargo workspace packages before either package build begins.

The release-content commit is
`ab02c05564404fe5a7c9ee4fbd34baa5ee6af30b`. Documentation-only qualification evidence recorded
afterward is not an artifact input.

## Qualification Contract

The clean release-content commit must pass:

1. the complete compiler gate in a disposable Cargo target;
2. repository metadata, performance helpers, pinned Unicode reproduction, formatting,
   warnings-denied all-target Clippy, every enabled locked workspace test, no-default-features
   checking, and warnings-denied Rust documentation;
3. deterministic out-of-tree website generation and repository whitespace validation;
4. two independent optimized package builds with byte-identical archives and recursively identical
   installed homes;
5. verified fresh installation and active-home replacement from the retained archive, followed by
   installation diagnosis, recursive identity comparison, and transaction-cleanup confirmation;
6. fresh installed-home checks for version, help, initialization, locked/offline check and test,
   deterministic graphs, native build, run, and direct execution;
7. every public example and the retained application-specific native output contracts, including
   bounded validation of the nondeterministic random example;
8. interactive LSP initialization, dynamic capability registration, semantic queries,
   edited-source diagnostics, recovery, and shutdown; and
9. proof that qualification commands do not mutate the installed home and that changing either the
   compiler or one standard-library source invalidates it.

## Qualification Evidence

The exact release-content commit passed the complete disposable compiler gate, including all
locked workspace tests, warnings-denied all-target Clippy, no-default-features checking, and
warnings-denied Rust documentation. Deterministic documentation generation produced 341 pages.

Qualification built the package twice in independent disposable directories. The archives were
byte-identical and the extracted homes were recursively identical. The retained archive then
passed fresh installation, active-home replacement, `doctor`, recursive home comparison,
transaction cleanup, every public example, application-specific native contracts, interactive
LSP checks, installed-home immutability checks, and compiler and standard-library tamper rejection.

The retained candidate is:

- path: `dist/nocter-v0.63.0-arm64-darwin.tar.gz`;
- byte length: `9423314`;
- archive SHA-256: `93a3c9bb6238d73dbbcc0aca24f2c93d71e32f252b31f1a2f24d528e0b264697`;
- compiler SHA-256: `0dbeb3a3124cda6c46fad640205bd9020a522ccc45a7b0bfc7bf000cb7b07539`;
- standard-library tree SHA-256:
  `e896536dbc901173a8793fc71899af9fd59aa0bad7bc0d48f3d0bfca4a109571`; and
- standard-library file count: `391`.

## Publication Boundary

Publication is explicitly authorized for this release and must reuse the exact retained qualified
archive without rebuilding it. The annotated source tag, GitHub Release asset, latest-release
endpoint, downloaded installed home, and source-identified Pages deployment require a separate
post-publication audit.
