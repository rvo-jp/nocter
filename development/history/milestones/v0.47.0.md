# v0.47.0: Canonical Asynchronous Byte I/O

**Status: implementation complete; release preparation next.**

## Outcome

v0.47.0 gives executor-safe byte streams one generic public contract. Unqualified `Reader` and
`Writer` become the canonical asynchronous interfaces, synchronous code uses explicit
`BlockingReader` and `BlockingWriter` contracts, and ordinary whole-stream and text adaptation is
implemented once as async interface defaults rather than once per concrete transport.

## Phase 0: Contract and Migration Inventory

**Status: complete.**

- Fix interface naming, execution kinds, receiver access, result and capture ownership, and default
  algorithm behavior before changing public declarations.
- Inventory every standard, example, compiler fixture, semantic-editor assertion, and behavior
  guide that currently uses `Reader` or `Writer`.
- Separate portable byte semantics from transport-specific timeout, readiness, framing, and close
  policy.
- Prove that existing async interface validation and dispatch are the only compiler mechanisms
  needed; do not add standard-name recognition.

Completion requires one cross-module design authority, an exact migration set, checked async
interface/default-method probes, formatting, deterministic documentation, and repository checks.

Phase 0 completed on 2026-09-11. The migration inventory contains the `std/io` declarations and
bodies, file buffers, filesystem helpers, JSON generation and its private sink adapter, TCP, TLS,
HTTP response bodies, nine public example source files, four native-session fixture families, and
the semantic-editor reference source. The adopted names follow the already-released concrete API
rule instead of adding an `Async` prefix to the canonical surface. A focused checker probe proves
that a generic async function can await a default async interface method which itself awaits a
required method twice. Existing witness matching already rejects an immediate method returning a
future as an async implementation, so the standard migration requires no parser, resolver,
checker, lowering, or editor special case.

## Phase 1: Explicit Synchronous Contracts

**Status: complete.**

- Rename the existing interfaces to `BlockingReader` and `BlockingWriter` without aliases.
- Migrate `File`, file buffers, filesystem helpers, JSON generation, concrete network and TLS
  witnesses, public examples, native fixtures, and editor expectations in one closed change.
- Keep every synchronous wait and `_blocking` method contract unchanged.

Completion requires the complete standard source and public examples to contain no old synchronous
interpretation of `Reader` or `Writer`.

Phase 1 completed on 2026-09-11. `BlockingReader` and `BlockingWriter` now own the synchronous
contracts without aliases. File and buffer implementations, filesystem composition, JSON output
and its private adapter, TCP, TLS, HTTP response bodies, all affected public examples, native
fixtures, semantic-editor fixtures, current cross-crate designs, and the public module example use
the explicit names. Searches over active source and current documentation find no remaining old
synchronous interpretation. The complete native-session suite, complete language-server suite,
and every public single-file and package example pass through ordinary interface selection.

## Phase 2: Canonical Async Interfaces and Defaults

**Status: complete.**

- Add async `Reader.read` and `Writer.write` requirements.
- Add generic async whole-stream, UTF-8, flush, text, and line defaults.
- Preserve invalid-read-count, partial-write, EOF, failure, cancellation, allocation, and UTF-8
  behavior through one portable implementation.
- Exercise generic async default dispatch and mismatched synchronous witnesses in compiler and
  native tests.

Phase 2 completed on 2026-09-11. `Reader` and `Writer` now own only asynchronous requirements and
defaults. Their bodies live in a new async-I/O source responsibility, while scratch initialization
and invalid-count construction moved to one pure helper visible to both execution surfaces. The
blocking helpers received explicit internal names rather than relying on overload selection.
A native memory reader and writer exercise required and default witness dispatch, multiple reads,
EOF, owned collection, UTF-8 validation, exact line bytes, stateless flush, impossible-count
failure, and ordinary awaited propagation. The existing checker probe and witness-mismatch test
confirm that execution kind comes from declarations and interface evidence rather than names.

## Phase 3: Transport Conformance and Deduplication

**Status: complete.**

- Make TCP and TLS implement both explicit blocking and canonical async contracts.
- Make HTTP `Response` implement both reader contracts.
- Delete response-specific ordinary collection and route its timeout variants through only the
  concrete timeout primitive plus shared collection policy.
- Keep transport timeout methods outside the base interfaces.

Phase 3 completed on 2026-09-11. `TcpStream` and `TlsStream` now implement both asynchronous and
explicit blocking byte contracts, and `Response` implements both reader contracts over its single
body cursor. The generic `Reader` defaults own ordinary whole-stream collection and UTF-8
conversion; the HTTP implementation retains only timeout-bearing collection. Blocking, async, and
timeout paths share one package-visible read-count validator, while framing, deadlines, and
transport progress remain concrete responsibilities. Generic async default dispatch and the
public asynchronous HTTP client both pass native execution.

## Phase 4: Application and Tooling Integration

**Status: complete.**

- Add a complete public program whose generic async byte algorithm works across more than one
  concrete reader or writer.
- Cover native interface dispatch, cancellation, EOF, failure, UTF-8 validation, and exact output.
- Qualify hover, completion, signature help, navigation, semantic tokens, and inlay hints through
  the ordinary checked contracts and implementation sources.
- Update the sole I/O, network, TLS, and HTTP behavior authorities without duplicating signatures.

Phase 4 completed on 2026-09-11. The public asynchronous HTTP package now defines one generic
per-read deadline adapter over `Reader` and uses it with both the server-side `TcpStream` and the
client-side `Response`. Its registered process contract executes the complete loopback exchange
without external network access. A semantic-editor source selects `Reader.read_to_string` through
a generic bound and qualifies hover, contract navigation, signature help, constrained completion,
semantic tokens, and inferred future, response, and string hints. The native async-I/O fixture and
transport suites jointly retain required/default dispatch, cancellation, partial progress, EOF,
stable failure, UTF-8 validation, and exact-byte evidence.

## Phase 5: Whole-Area Review and Release Readiness

**Status: complete.**

- Review for compatibility aliases, duplicate collection loops, source-name inference, concrete
  type branches, blocking operations reachable from futures, recomputed witness selection, reverse
  dependencies, and caller-trust contracts.
- Run the complete compiler, documentation, standard-library, public-example, formatting, and
  repository gates.
- Stop before changing release identity or publishing.

Phase 5 completed on 2026-09-11. The [final review](../reviews/v0.47.0-phase-5.md) traces the single
authorities for callable semantics, generic I/O policy, transport progress, HTTP framing, frozen
dispatch, suspension, and semantic-editor projection. It found no remaining compatibility alias,
ordinary collector duplication, name-based compiler behavior, blocking async witness, or repeated
dispatch selection. The complete disposable-target compiler gate passes after splitting one
oversized LSP assertion function into a helper that reuses the same semantic generation. Release
identity and artifact qualification remain intentionally unchanged.

## Phase 6: Compiler Convention Authority Closure

**Status: complete.**

- Replace remaining semantic comparisons against compiler-known source spellings with typed
  language or syntax vocabulary.
- Give package directives, package record fields, the standard package, module names, and physical
  source layout one owner each.
- Remove caller-trust construction of exact dependency selections and command-name
  reclassification at the CLI boundary.
- Review parser, package, discovery, checking, presentation, command, and LSP paths for duplicate
  convention authorities and dependency reversal.

Phase 6 completed on 2026-09-11. The
[authority-closure review](../reviews/v0.47.0-phase-6.md) records the findings, remediation, and
whole-workspace evidence. Compiler-known spellings now cross responsibility boundaries as enums,
validated values, or language-owned layout constants. The protocol-only LSP crate remains language
independent: its watched-file builder receives a glob selected by the language server.

## Phase 7: Source Snapshot Identity Closure

**Status: complete.**

- Prevent equal local source indexes in independent identity domains or changed source revisions
  from aliasing, while retaining identity across metadata-only generations.
- Make independently parsed trees for the same source reject each other's node identities.
- Replace public source-backed syntax helpers that trust separate source and tree arguments with a
  proved binding.
- Bind target selection to its exact compilation target and syntax snapshots.
- Reuse package resolution's exact root syntax during module discovery instead of reparsing it.
- Remove alternate compile-input construction paths that could bypass or silently recompute the
  completed selection.

Phase 7 completed on 2026-09-12. The
[source snapshot review](../reviews/v0.47.0-phase-7.md) records the original caller-trust hole, the
compact identity model, migration of source-backed decoders and projections, compile-input
validation, revision-family identity reuse, and whole-workspace evidence. Source and node integrity
identities remain opaque and do not enter deterministic diagnostics or public semantic surfaces.

## Non-goals

- asynchronous regular-file or directory operations;
- a blocking-worker pool, detached tasks, threads, or parallel execution;
- timeout or cancellation policy in the base byte interfaces;
- generic datagram I/O through stream interfaces;
- seek, random access, vectored I/O, or memory mapping;
- effect-polymorphic interface methods;
- compatibility aliases for the previous synchronous interface names.
