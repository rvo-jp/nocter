# v0.68.0 Release Preparation

**Status: qualified with public metadata prepared (2026-09-24).** This record owns the v0.68.0
release-content identity, qualification contract, and retained local artifact evidence. Publication
and its public audit remain separate work.

## Release Boundary

v0.68.0 closes production native performance: one Machine-owned draft-to-freeze optimization
boundary, conservative effect and call-boundary legality, dense reachability pruning, local storage
and address proofs, physical-storage aliases, direct aggregate construction, Machine-owned bounds
dispositions, and an external complete-application workload comparison. The
[milestone](v0.68.0.md) owns the phase scope, the
[Phase 5 review](../reviews/v0.68.0-phase-5.md) records the final authority audit, and the
[Phase 4 review](../reviews/v0.68.0-phase-4.md) records exact performance evidence. Public candidate
notes are [`../../../releases/v0.68.0.md`](../../../releases/v0.68.0.md).

## Candidate Identity

The release inputs agree on `0.68.0`:

- `development/packaging/VERSION`, the sole authored release-version input;
- the `#package` version in `std/index.nct`;
- the archive name and installed manifest derived from `VERSION` and `RELEASE.json`; and
- compiler and language-server reporting derived from the validated installed home.

`RELEASE.json` remains the machine authority for version-independent license, host, target, and
archive-layout metadata. Qualification ran from the clean release-content commit recorded below.

## Qualification Contract

The clean release-content commit must pass:

1. the complete compiler gate in a disposable Cargo target;
2. repository metadata, benchmark helpers, pinned Unicode reproduction, formatting,
   warnings-denied all-target Clippy, every enabled locked workspace test, no-default-features
   checking, and warnings-denied Rust documentation;
3. deterministic out-of-tree website generation and repository whitespace validation;
4. two independent optimized package builds with byte-identical archives and recursively identical
   installed homes;
5. verified fresh installation and active-home replacement from the retained archive, followed by
   installation diagnosis, recursive identity comparison, and transaction-cleanup confirmation;
6. fresh installed-home checks for version, help, initialization, locked/offline check and test,
   deterministic graphs, native build, run, and direct execution;
7. every public example plus the complete process, filesystem, network, TLS, HTTP, durable-state,
   configuration, and service application contracts;
8. interactive LSP initialization, dynamic capability registration, semantic queries,
   edited-source diagnostics, recovery, and shutdown;
9. structural optimizer evidence and the seven-workload external runner without benchmark-only
   production behavior; and
10. proof that qualification commands do not mutate the installed home and that changing either
    the compiler or one standard-library source invalidates it.

## Qualification Evidence

Release-content commit `8b14736dffecbc15d2afc83061a253e1e98da3d4` passed the complete compiler
gate in a disposable target. The gate covered repository metadata, benchmark helpers, pinned
Unicode reproduction, formatting, warnings-denied all-target Clippy, every enabled locked workspace
test, no-default-features checking, warnings-denied Rust documentation, deterministic 372-page
website generation, and repository whitespace validation. The successful run included 54 Machine
tests, 117 ARM64 tests, 61 analysis tests, 38 CLI tests, 62 command tests, 126 native-session tests,
and 118 language-server tests.

Deterministic qualification built the optimized compiler twice in independent temporary Cargo
targets. The compressed archives matched byte for byte, their extracted `.nocter/` homes matched
recursively, and the retained archive passed verified fresh installation and active-home
replacement. The retained candidate is:

- archive: `dist/nocter-v0.68.0-arm64-darwin.tar.gz`;
- byte size: 9,561,026;
- SHA-256: `36c26be4910a07bc7cb36ffbec30c36d4280f8d1a18a72da40d1775ecbc3a775`;
- compiler SHA-256: `55088c91851b198e88cb4ab1297d9913b9823b100d84b08dd1545440c393d9e0`;
- standard-library tree SHA-256:
  `fb5c772c95099f094cf5552817782f2144c4ff03c14a590dc313eb43f787d8d2`; and
- standard library: exactly 478 regular files.

Qualification passed installation diagnosis, transaction cleanup, version and help, package
initialization, locked/offline checking and testing, deterministic package graphs, native build,
run, and direct execution, every public example, application-specific operational contracts,
interactive LSP initialization and editor queries, edited-source diagnostics and recovery,
installed-home immutability, and independent compiler and standard-library tamper rejection.

The retained installed candidate was then compared directly with the downloaded, digest-verified
v0.67.0 release on the same Apple M1 host. The ordinary external runner used two warmups and seven
alternating samples for each of the seven complete native workloads. All observable results passed.
The final candidate reproduced the Phase 4 executable sizes exactly: four images were 3.67–6.16%
smaller and three were unchanged. Runtime medians improved by 6.22% for line frequency, 7.85% for
JSON normalization, 12.93% for archive inspection, 7.85% for binary records, and 14.06% for async
HTTP. The subprocess pipeline differed by -0.04%, while the bounded service differed by +0.85%;
both remain dominated by external work and deliberate waits and are treated as effectively flat.
The comparison recorded candidate compiler digest
`55088c91851b198e88cb4ab1297d9913b9823b100d84b08dd1545440c393d9e0`, standard-library tree
`fb5c772c95099f094cf5552817782f2144c4ff03c14a590dc313eb43f787d8d2`, clean repository revision
`7d34a0207cf635c6c2eaa53fd55d47e33ec63117`, exact source and input digests, and every sample order.

## Publication Boundary

Publication must reuse the exact retained qualified archive without rebuilding it. The annotated
tag, single GitHub Release asset, latest-release endpoint, downloaded installed home,
push-triggered workflows, and source-identified Pages deployment require a separate
post-publication audit. The tag and asset are immutable by project policy; any correction requires
a new release identity.
