# v0.66.0 Release Preparation

**Status: published and externally audited (2026-09-22).** This record owns the v0.66.0
release-content identity, qualification contract, and retained local artifact evidence. The
[publication audit](../release-audits/v0.66.0.md) owns the public tag, asset, workflow, and Pages
evidence.

## Release Boundary

v0.66.0 closes durable filesystem replacement, bounded recoverable journal framing, one
single-writer byte store, bounded compaction, persistent graceful-service state, and the editor and
native coverage for those contracts. The [milestone](v0.66.0.md) owns the completed phase scope.
The [Phase 5 review](../reviews/v0.66.0-phase-5.md) records the final authority model and resolved
findings. Public candidate notes are
[`../../../releases/v0.66.0.md`](../../../releases/v0.66.0.md).

## Candidate Identity

The release inputs agree on `0.66.0`:

- `development/packaging/VERSION`, the sole authored release-version input;
- the `#package` version in `std/index.nct`;
- the archive name and installed manifest derived from `VERSION` and `RELEASE.json`; and
- compiler and language-server reporting derived from the validated installed home.

`RELEASE.json` remains the machine authority for version-independent license, host, target, and
archive-layout metadata. Qualification verifies that its license agrees with the shipped legal
files and all Cargo workspace packages before either package build begins.

The release-content commit is `e3f499b5d38d87073fde1c06e1c474772a1c7752`. Documentation-only
qualification evidence recorded afterward is not an artifact input.

## Qualification Contract

The clean release-content commit must pass:

1. the complete compiler gate in a disposable Cargo target;
2. repository metadata, performance helpers, pinned Unicode reproduction, formatting,
   warnings-denied all-target Clippy, every enabled locked workspace test, no-default-features
   checking, and warnings-denied Rust documentation;
3. deterministic out-of-tree website generation and repository whitespace validation;
4. two independent optimized package builds with byte-identical archives and recursively identical
   installed homes;
5. verified fresh installation and active-home replacement from the retained archive, followed by
   installation diagnosis, recursive identity comparison, and transaction-cleanup confirmation;
6. fresh installed-home checks for version, help, initialization, locked/offline check and test,
   deterministic graphs, native build, run, and direct execution;
7. every public example and retained application-specific native output contract, including the
   durable store and complete stateful HTTP service recovery paths;
8. interactive LSP initialization, dynamic capability registration, semantic queries,
   edited-source diagnostics, recovery, and shutdown; and
9. proof that qualification commands do not mutate the installed home and that changing either the
   compiler or one standard-library source invalidates it.

## Qualification Evidence

Release-content commit `e3f499b5d38d87073fde1c06e1c474772a1c7752` passed the complete compiler
gate in a disposable target. The gate covered repository metadata, performance helpers, pinned
Unicode reproduction, formatting, warnings-denied all-target Clippy, every enabled locked
workspace test, no-default-features checking, warnings-denied Rust documentation, deterministic
363-page website generation, and repository whitespace validation. The successful run included 61
analysis tests, 62 command tests, 122 native-session tests, and 118 language-server tests. Its
native paths covered durable replacement, bounded store recovery, compaction, persistent graceful
service state, and the complete HTTP service example.

Deterministic qualification built the optimized compiler twice in independent temporary Cargo
targets. The compressed archives matched byte for byte, their extracted `.nocter/` homes matched
recursively, and the retained archive passed verified fresh installation and active-home
replacement. The retained candidate is:

- archive: `dist/nocter-v0.66.0-arm64-darwin.tar.gz`;
- byte size: 9,477,116;
- SHA-256: `d5476913c5bf8033f6b9e2b6844a42615b166a3af98b02e95af34043539cf87c`;
- compiler SHA-256: `1cc33278b3a598dd6c991abde8426edf3becf03a000b3ef4875cd53a9ee05380`;
- standard-library tree SHA-256:
  `6d375c76935fe95745539c4a3ccdc05d17ade4cbd9e80dcedabe36323a35a8e1`; and
- standard library: exactly 454 regular files.

Qualification passed installation diagnosis, transaction cleanup, version and help, package
initialization, locked/offline checking and testing, deterministic package graphs, native build,
run, and direct execution, every public example, application-specific native contracts,
interactive LSP initialization and editor queries, edited-source diagnostics and recovery,
installed-home immutability, and independent compiler and standard-library tamper rejection.

## Publication Boundary

Publication reused the exact retained qualified archive without rebuilding it. The annotated tag,
single GitHub Release asset, latest-release endpoint, downloaded installed home, push-triggered
workflows, and source-identified Pages deployment passed the
[post-publication audit](../release-audits/v0.66.0.md). The tag and asset are immutable by project
policy; any correction requires a new release identity.
