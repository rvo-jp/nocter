# v0.51.0 Release Preparation

**Status: qualified; publication authorized.** This record owns the release-candidate identity,
qualification contract, retained local artifact evidence, and publication boundary. Publication
must reuse the retained qualified archive without rebuilding or replacing it.

## Release Boundary

v0.51.0 replaces parallel or implicit execution classification with one time-aware semantic model.
Authored callable guarantees, inferred execution facts, immediate invocation, deferred future
drive, cancellation, and destruction retain distinct authority. One checking-owned relation graph
computes implementation facts, while later stages consume frozen decisions.

Closed primitive roles provide typed runtime facts through one exhaustive profile, and target
validation compares those facts with authored standard declarations once. Ownership-selected
destruction dependencies participate in the same execution relation as ordinary calls. Editor
presentation remains source-contract-only and cannot synthesize modifiers from implementation
facts.

The [milestone](v0.51.0.md) owns the completed implementation. The [closure
review](../reviews/v0.51.0-phase-5.md) records the final boundary corrections and complete compiler
gate. Public candidate notes are [`../../../releases/v0.51.0.md`](../../../releases/v0.51.0.md).
Root downloads, specification publication status, and the public release index remain at v0.50.0
until publication.

## Candidate Identity

The release inputs agree on `0.51.0`:

- `development/packaging/VERSION`, the sole authored release-version input;
- the `#package` version in `development/std/index.nct`;
- the archive name and installed manifest derived from `VERSION` and `RELEASE.json`;
- compiler and language-server reporting derived from the validated installed home.

`RELEASE.json` remains the machine authority for version-independent license, host, target, and
archive-layout metadata. Qualification verifies that its license agrees with the shipped legal
files and all Cargo workspace packages before either package build begins.

The qualified release-content commit is
`c9f3b6bf367619c6ba009e99dbda255b16629fc1`. Documentation-only qualification evidence recorded
after this commit is not an artifact input.

## Qualification Contract

The clean release-content commit must pass:

1. the complete compiler gate in a disposable Cargo target;
2. repository metadata, benchmark helpers, pinned Unicode reproduction, formatting,
   warnings-denied all-target Clippy, every enabled locked workspace test, no-default-features
   checking, and warnings-denied Rust documentation;
3. deterministic out-of-tree website generation and repository whitespace validation;
4. two independent optimized package builds with byte-identical archives and recursively identical
   installed homes;
5. fresh installed-home checks for version, diagnosis, help, initialization, locked/offline check
   and test, deterministic package graphs, native build, run, and direct execution;
6. every public example and the exact filesystem, streaming, network, HTTP, TLS, and process
   contracts, including the bounded asynchronous file-report success and failure paths;
7. framed LSP analysis of installed public contracts and implementation sources;
8. proof that qualification commands do not mutate the installed home and that changing either the
   compiler or one standard-library source invalidates it.

## Qualification Evidence

The exact release-content commit passed the complete compiler gate in a disposable Cargo target.
The gate included repository metadata, performance helpers, pinned Unicode reproduction,
formatting, warnings-denied all-target Clippy, every enabled locked workspace test,
no-default-features checking, warnings-denied Rust documentation, deterministic out-of-tree website
generation, and repository whitespace validation.

Two independent optimized builds produced byte-identical archives and recursively identical
installed homes. A fresh extraction passed safe-layout and ARM64 checks, version, doctor, help,
initialization, locked/offline check and test, deterministic graphs, native run and build, direct
execution, every public example, the bounded asynchronous file-report success and missing-root
cleanup paths, framed LSP analysis, installed-home immutability, and independent compiler and
standard-library tamper rejection.

The retained candidate is:

- archive: `dist/nocter-v0.51.0-arm64-darwin.tar.gz`;
- byte size: 9,077,529;
- archive SHA-256: `396c8cf0fe39b2d94a519e14c4c5cce13b9d3571d2fc2e7cb99568ef8373f727`;
- compiler SHA-256: `936e37064d3db8f40144ccf606c1eea2455dd948069a04fb5a2cfcc2e8b0be99`;
- standard-library tree SHA-256:
  `91985c0e5356e022989f178932a2a3594b670c1a5e1610a22bf1480170a3014b`;
- standard library: exactly 307 regular files.

## Publication Boundary

Do not create a tag, push commits, upload an asset, deploy documentation, or update public latest-
version entry points until qualification evidence is complete and publication is explicitly
requested. Publication must reuse the retained archive produced by qualification.
