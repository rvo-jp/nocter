disallowed-types = [
    { path = "nocter_model::TypeAuthority", reason = "semantic construction ownership is restricted to model, declarations, and checking" },
    { path = "nocter_model::TypeTransaction", reason = "downstream phases consume the immutable TypeStore contract" },
    { path = "nocter_model::ClosureSequence", reason = "closure construction storage is restricted to model and checking" },
    { path = "nocter_persistent::PersistentMap", reason = "persistent storage mechanics must remain behind semantic authorities" },
    { path = "nocter_persistent::PersistentVector", reason = "persistent storage mechanics must remain behind semantic authorities" },
]

disallowed-methods = [
    { path = "nocter_declaration_lowering::lower_compile_unit_declarations_recovering", reason = "semantic query composition is private to nocter-compiler-computation" },
    { path = "nocter_declaration_lowering::lower_incomplete_body_declarations_recovering", reason = "incomplete editor traversal is private to nocter-compiler-computation" },
    { path = "nocter_checking::prepare_program_checking_recovering", reason = "semantic query composition is private to nocter-compiler-computation" },
    { path = "nocter_checking::prepare_analysis_program_checking_recovering", reason = "incomplete editor traversal is private to nocter-compiler-computation" },
    { path = "nocter_checking::check_prepared_program_recovering", reason = "semantic query composition is private to nocter-compiler-computation" },
    { path = "nocter_checking::analyze_prepared_program_bodies", reason = "incomplete editor traversal is private to nocter-compiler-computation" },
    { path = "nocter_source_index::SourceIndex::issues", reason = "source projection integrity is consumed only by the analysis evidence kernel" },
]
