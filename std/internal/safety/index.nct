//! Target-neutral safety termination boundaries used by standard containers.

see ./darwin.nct

pub(/) noalloc func bounds_abort(): never

/// Terminates after detecting an internal standard-library invariant violation.
pub(/) noalloc func invariant_abort(): never
